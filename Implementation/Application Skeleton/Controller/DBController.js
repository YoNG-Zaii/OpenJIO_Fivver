/**
 * The control class for data requests to and from database.
 */
const DBController = () => {

    /**
     * Save data inside the database.
     * @param {String} category The category of data.
     */
    const saveInDB = (category) => { }

    /**
     * Retrieve data from the database.
     */
    const retrieveFromDB = (category) => { }
}

export default DBController